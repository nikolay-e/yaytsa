apiVersion: v1
kind: Service
metadata:
  name: {{ include "jellyfin-miniclient.fullname" . }}
  labels:
    {{- include "jellyfin-miniclient.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jellyfin-miniclient.selectorLabels" . | nindent 4 }}
