# Jellyfin Server Configuration (REQUIRED)
# This must be configured at deployment time (Docker/Kubernetes environment variables)
YAYTSA_SERVER_URL=https://your-jellyfin-server.com

# E2E Test Credentials (for Playwright tests only)
YAYTSA_TEST_USERNAME=test-user
YAYTSA_TEST_PASSWORD=test-password
BASE_URL=http://localhost:5173

# CSP (Content Security Policy) Mode
# Controls security policy strictness for connect-src directive:
#   - "strict": Only allow pre-configured YAYTSA_SERVER_URL domain
#   - "relaxed": Allow all domains (recommended for private deployments ~10 users)
#   - "auto" (default): strict if YAYTSA_SERVER_URL is set, otherwise relaxed
CSP_MODE=relaxed
