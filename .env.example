# Jellyfin Server Configuration
# ==============================================================================
# SECURITY WARNING: Never commit .env files with real credentials to git!
# ==============================================================================

# Copy this file to .env and fill in your actual values
# For the web package, also create packages/web/.env with VITE_ prefix

# ----------------------------------------------------------
# Server Connection
# ----------------------------------------------------------

# URL of your Jellyfin server (no trailing slash)
# SECURITY: Always use HTTPS in production (HTTP only for localhost)
# You can also enter this on the login screen
JELLYFIN_SERVER_URL=https://your-jellyfin-server.com

# ----------------------------------------------------------
# Client Identification
# ----------------------------------------------------------

# Client name to display in Jellyfin sessions
# Defaults to "Jellyfin Mini Client"
JELLYFIN_CLIENT_NAME="Jellyfin Mini Client"

# Device name to display in Jellyfin sessions
# Defaults to system hostname
JELLYFIN_DEVICE_NAME=Development

# ----------------------------------------------------------
# Test Credentials (for E2E integration tests)
# ----------------------------------------------------------
# Username and password for testing against real Jellyfin server
# SECURITY: Only use test accounts, never production credentials
JELLYFIN_TEST_USERNAME=your-test-username
JELLYFIN_TEST_PASSWORD=your-test-password

# ----------------------------------------------------------
# Web Package Variables (for Vite)
# ----------------------------------------------------------
# If running the web package directly, create packages/web/.env with:
#
# VITE_JELLYFIN_SERVER_URL=https://your-jellyfin-server.com
# VITE_JELLYFIN_CLIENT_NAME=Jellyfin Mini Client
# VITE_JELLYFIN_DEVICE_NAME=Web Browser
